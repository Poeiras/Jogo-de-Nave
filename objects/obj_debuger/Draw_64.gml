/// @description
if(room = rm_base)
{
	var da = "Direção do atirador: "+	string(obj_inimi_00.direction);
	var dc = "Direção do colisor: " +	string(obj_inimi_01.direction);
	draw_text(global.metadex,10,da)
	draw_text(global.metadex,30,dc);	
	// comentário do brunão
}
